package com.amiabledata.archertracker.stage;

public class TeamQualificationStage extends Stage {
    public TeamQualificationStage() {
        setStage(new String());
    }

    public TeamQualificationStage(TeamQualificationStage value) {
        setStage(value.getStage());
    }
}
